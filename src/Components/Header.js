import React, { Component } from 'react';

const Header = ({total}) => (
            <div>
                <h5>Rainforest TM</h5>
                <div>Total: ${total}</div>
            </div>
    
)

export default Header